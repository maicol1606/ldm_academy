import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            light: "#F2B274",
            main: "#FF7F02",
            dark: "#e87402",
            contrastText: "#FFF",
        }
    }
})